// Generated by CoffeeScript 1.8.0
(function() {
  var App;

  App = typeof exports !== "undefined" && exports !== null ? exports : window;

  App.coffeeMind = App.coffeeMind || {};

  App.coffeeMind.screens = App.coffeeMind.screens || {};

  App.coffeeMind.screens["highscore"] = (function() {
    var enterScore, populateList, run, setup, _$, _firstRun, _game, _numScores, _storage;
    _$ = {};
    _game = {};
    _storage = {};
    _numScores = 10;
    _firstRun = true;

    /*
     * @method setup
     * @param {jQuery} $
     */
    setup = function($) {
      var backButton;
      _$ = $;
      _game = App.coffeeMind.game;
      _storage = App.coffeeMind.storage;
      backButton = _$("#highscore footer button[name=back]");
      return backButton.bind('click', function() {
        return _game.showScreen('main-menu');
      });
    };

    /*
     * @method run
     * @param {Int} score
     */
    run = function($, score) {
      if (_firstRun === true) {
        setup($);
        _firstRun = false;
      }
      populateList();
      if (typeof score !== 'undefined') {
        enterScore(score);
      }
      return null;
    };

    /*
     * @method populateList
     */
    populateList = function() {
      return null;
    };

    /*
     * @method score
     * @param {Int} score
     */
    enterScore = function(score) {
      return null;
    };
    return {
      run: run
    };
  })();

}).call(this);

//# sourceMappingURL=screen.highscore.js.map
